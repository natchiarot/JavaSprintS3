import java.util.ArrayList;

public class Demo {
    public static void main(String[] args) {
    // patronList.add("");
    // patronList.add("");

    // for (int i = 0; i < patronList.size(); i++) {
    //     System.out.println("Index " + i + ": " + patronList.get(i));
    // }

    // Patron patron = // get the specific patron object;
    // String searchTerm = // specify the search term;
    // ArrayList<Book> titleResults = patron.searchBorrowedBookTitle(searchTerm);
    // ArrayList<Book> authorResults = patron.searchBorrowedBookAuthor(searchTerm);
    // Book isbnResult = patron.searchBorrowedBookISBN(Integer.parseInt(searchTerm));
}
}
